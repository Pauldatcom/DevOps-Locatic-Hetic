# infra/kubernetes (legacy)

Les manifests Kubernetes ont ete unifies sous [`deploy/k8s/`](../../deploy/k8s/).

- Application : `deploy/k8s/app/`
- Nginx reverse proxy : `deploy/k8s/nginx/`

Voir [`docs/kubernetes.md`](../../docs/kubernetes.md).

Ne plus ajouter de manifests ici.
