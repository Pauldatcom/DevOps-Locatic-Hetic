apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - app.yaml
  - configmap.yaml
  - nginx.yaml
  - pvc.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: locatic
    includeSelectors: true
