apiVersion: v1
kind: Service
metadata:
  name: locatic
  labels:
    app.kubernetes.io/name: locatic
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: locatic
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP